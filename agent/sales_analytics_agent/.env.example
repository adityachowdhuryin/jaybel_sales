# Agent Engine deploy / local smoke tests (analytics in GCP — not Postgres)

GOOGLE_CLOUD_PROJECT=jaybel-dev
GOOGLE_CLOUD_LOCATION=us-central1

# After deploy, record in agent/AGENT_ENGINE_RESOURCE.env (not frontend Firebase config)
# AGENT_ENGINE_RESOURCE=projects/115724636423/locations/us-central1/reasoningEngines/<ID>

# Phase C local API reads the same resource from backend/.env
